Продолжай развитие текущего приложения-блокнота для iPad. Не переписывай проект с нуля и не ломай существующую архитектуру. Сохрани текущую основу: React + TypeScript + Vite, PWA-формат, офлайн-логику, локальное хранение через IndexedDB, текущую feature-структуру и уже существующие базовые сценарии.

Нужен не временный прототип, а аккуратная, расширяемая и цельная реализация, которая визуально и по UX ощущается как настоящий красивый блокнот, а не как сайт.

==================================================
ГЛАВНАЯ ЦЕЛЬ
==================================================

Превратить текущее приложение в красивый, атмосферный, русскоязычный офлайн-блокнот для iPad в смешанной стилистике:
- космос;
- мото;
- современный визуальный стиль;
- ощущение реального блокнота;
- удобное сенсорное управление;
- поддержка текста, рисования, вставок, фото, файлов и перелистывания страниц.

==================================================
ОБЯЗАТЕЛЬНЫЕ ОГРАНИЧЕНИЯ
==================================================

1. Не ломай существующую архитектуру проекта.
2. Сохрани:
- React + TypeScript + Vite;
- PWA;
- офлайн-работу;
- IndexedDB;
- текущую структуру app/features/shared;
- уже существующие возможности, если они не противоречат новому UX.
3. Все тексты интерфейса должны быть на русском языке.
4. Интерфейс должен быть адаптирован под iPad и сенсорное управление.
5. Одновременно он должен оставаться usable на компьютере с мышкой.
6. Не делай все в одном гигантском компоненте.
7. Все новые сущности, конфиги, ассеты и UI-блоки нужно разложить по логичной архитектуре.

==================================================
ОБЩАЯ ВИЗУАЛЬНАЯ КОНЦЕПЦИЯ
==================================================

Сделай единый визуальный стиль приложения в теме “космос + мото”.

Важно:
- темная база интерфейса;
- хорошая читаемость;
- атмосферные фоны;
- красивые текстуры;
- стильные кнопки и панели;
- визуально дорогой, собранный, не перегруженный UI.

Интерфейс должен ощущаться как премиальный личный блокнот для iPad, а не как обычный веб-интерфейс.

==================================================
ГЛАВНАЯ СТРАНИЦА
==================================================

Переделай главный экран.

Структура:
- сверху по центру крупный заголовок: "Мои блокноты";
- ниже кнопки:
  - "Создать"
  - "Настроить фон приложения"
- ниже отображаются все созданные блокноты.

Но сами блокноты на главной странице должны выглядеть не как обычные карточки сайта, а как реальные блокноты.

Для каждого блокнота на главной странице нужно показывать:
- прямоугольную форму блокнота;
- цвет;
- текстуру/структуру обложки;
- внешний вид обложки;
- переплет, если он выбран;
- кольца, пружину или другой визуальный признак переплета, если это выбрано.

На самом блокноте по центру должно быть:
- только название блокнота, если пользователь его задал;
- если названия нет, то на обложке ничего не писать.

На главной странице НЕ нужно отображать:
- тип блокнота;
- тип бумаги;
- выбранную ручку;
- служебные подписи;
- нижние названия под карточками;
- лишние метаданные.

Главная страница должна восприниматься как набор реальных блокнотов, а не как список карточек с технической информацией.

==================================================
НАСТРОЙКА ФОНА ПРИЛОЖЕНИЯ
==================================================

Настройка фона приложения не должна быть просто выбором цветов.

Сделай настройку фона через визуальные карточки-превью с реальными изображениями сцен.

Пользователь должен иметь возможность:
- выбрать фон из готовых пресетов;
- загрузить собственное изображение для фона приложения;
- сохранить пользовательский фон локально;
- восстановить его после перезагрузки.

Исправь и реализуй такие фоновые пресеты как полноценные сцены, а не как абстрактные цветовые оттенки:

1. "Глубокий космос"
Должно быть видно:
- звезды;
- туманности;
- глубина космоса.

2. "Метеоритный поток"
Должно быть видно:
- метеориты;
- космос;
- ощущение потока метеоров.

3. "Ночная трасса"
Должно быть видно:
- ночная дорога/трасса;
- звездная ночь;
- ночное небо.

4. "Северный двигатель"
Должно быть видно:
- северное сияние;
- элементы двигателя/механики на фоне.

5. "Пыль Сатурна"
Должно быть видно:
- планета Сатурн;
- кольца Сатурна;
- космическая пыль;
- космический мусор.

6. "Карбоновый дрифт"
Должен быть фон, реально соответствующий названию:
- карбон;
- динамичный технический стиль;
- мото/авто настроение;
- не просто серо-черный градиент.

Добавь отдельные настройки:
- сила затемнения фона;
- сила размытия фона.

Пользователь должен иметь возможность:
- уменьшать или усиливать затемнение;
- уменьшать или усиливать blur/размытие;
- настраивать видимость фона так, чтобы он не мешал читаемости интерфейса.

Если нужно, используй overlay поверх фонового изображения.

==================================================
СОЗДАНИЕ БЛОКНОТА
==================================================

Добавь полноценный экран или модальное окно создания блокнота.

При создании блокнота пользователь должен иметь возможность выбрать:

- название блокнота;
- цвет блокнота;
- тип бумаги;
- стиль;
- тип блокнота;
- внешний вид обложки;
- тип переплета;
- инструмент по умолчанию.

Все варианты выбора должны быть максимально визуальными, а не просто списками текста.

==================================================
СТИЛИ БЛОКНОТА
==================================================

Стили при создании блокнота не должны быть просто цветными прямоугольниками.

Сделай варианты стилей в виде визуальных карточек, где есть:
- символы;
- изображения;
- декоративные элементы;
- понятное мини-превью образа.

Пользователь должен выбирать стиль по визуальному образу.

==================================================
ТИПЫ БЛОКНОТА
==================================================

Расширь типы блокнотов.

Добавь как минимум:
- ежедневник;
- блокнот с пружиной;
- планер;
- записная книжка.

Если в проекте уже есть другие типы, не удаляй их — дополни список.

==================================================
ВНЕШНИЙ ВИД ОБЛОЖКИ
==================================================

Добавь отдельную настройку внешнего вида блокнота / обложки.

Это то, как блокнот выглядит снаружи:
- на главной странице;
- при открытии блокнота.

Поддержи как минимум такие варианты:
- кожа;
- картон;
- рисунки;
- узоры;
- декоративные обложки;
- другие готовые варианты;
- загрузка собственного изображения для обложки.

Требования:
- внешний вид выбирается при создании;
- его можно изменить позже;
- на главной странице видно превью обложки;
- при открытии блокнота сохраняется тот же визуальный образ;
- пользователь может выбрать готовый вариант или загрузить свою картинку;
- всё сохраняется локально.

==================================================
ПЕРЕПЛЕТ
==================================================

Добавь выбор книжного переплета.

Поддержать варианты:
- пружина;
- скрепка;
- кольца.

Требования:
- переплет выбирается при создании блокнота;
- отображается на главной странице;
- отображается при открытии блокнота;
- влияет на внешний вид блокнота;
- не должен быть просто текстовым полем;
- должен визуально быть частью макета блокнота.

==================================================
НАСЛЕДОВАНИЕ ПАРАМЕТРОВ ПРИ СОЗДАНИИ
==================================================

Когда пользователь создает блокнот, после открытия он сразу должен получать:
- выбранный тип бумаги;
- выбранный цвет бумаги;
- выбранный стиль;
- выбранный внешний вид;
- выбранный переплет;
- стартовый инструмент по умолчанию.

Новый блокнот должен открываться сразу с нужным визуальным и структурным состоянием, а не с дефолтами.

==================================================
КАТАЛОГ ТИПОВ БУМАГИ
==================================================

Нужен большой встроенный и расширяемый каталог типов бумаги.

Под требованием “вообще все существующие типы бумаги” понимай:
- максимально широкий встроенный каталог;
- расширяемую архитектуру пресетов;
- возможность легко добавлять новые типы в будущем;
- конфиги бумаги должны быть вынесены в отдельные данные/модели, а не зашиты в UI.

Минимально поддержать:
- чистая;
- линованная;
- в клетку;
- в точку;
- миллиметровка;
- инженерная сетка;
- изометрическая;
- музыкальная;
- каллиграфическая;
- для скетчинга;
- крафтовая;
- кремовая;
- тёмная;
- ежедневник;
- планер;
- чек-лист;
- Cornell notes;
- storyboard;
- комикс-разметка;
- школьная;
- лабораторная;
- архитектурная;
- чертёжная.

Важно:
- у бумаги должны быть пресеты;
- у бумаги должны быть визуальные параметры;
- должна быть возможность сменить цвет листа без потери линий, сетки, точек, текстуры и рельефа.

==================================================
КАТАЛОГ РУЧЕК, КИСТЕЙ И ИНСТРУМЕНТОВ
==================================================

Нужен большой встроенный и расширяемый каталог инструментов.

Под требованием “вообще все существующие ручки” понимай:
- максимально широкий встроенный каталог;
- расширяемую архитектуру пресетов;
- отдельные конфиги и типы для инструментов;
- отсутствие жесткой логики “можно выбрать только один инструмент на весь блокнот”.

Default tool может существовать только как стартовый инструмент при открытии блокнота, но потом пользователь должен свободно переключаться на любые инструменты.

Минимально поддержать:
- шариковую ручку;
- гелевую ручку;
- перьевую ручку;
- капиллярную ручку;
- линер;
- маркер;
- текстовыделитель;
- карандаш;
- механический карандаш;
- кисть;
- каллиграфическую кисть;
- тонкую кисть;
- толстую кисть;
- фломастер;
- уголь;
- ластик.

Для инструментов заложи настройки:
- цвет;
- толщина;
- прозрачность;
- тип штриха;
- сглаживание, если уместно.

==================================================
ОТКРЫТИЕ БЛОКНОТА
==================================================

Когда пользователь нажимает на блокнот, нельзя показывать промежуточный экран.

Нужно убрать:
- надпись "Страницы";
- надпись "Файлы блокнота";
- кнопку "Создать страницу";
- промежуточный экран со списком страниц как первый экран.

Вместо этого:
- блокнот должен сразу открываться на первой пустой странице;
- страница должна сразу быть в выбранном формате;
- никаких лишних надписей на странице.

То есть пользователь должен сразу попадать в рабочее пространство блокнота.

==================================================
СТРАНИЦА ДОЛЖНА ВЫГЛЯДЕТЬ КАК ЛИСТ БЛОКНОТА
==================================================

Открытая страница должна выглядеть как лист реального блокнота, а не как веб-страница.

Требования:
- один лист = одна страница;
- страница не должна прокручиваться вниз как сайт;
- вертикальный бесконечный скролл убрать;
- новая страница появляется через перелистывание;
- новые страницы наследуют формат первой страницы;
- пользователь должен ощущать работу с листами, а не с веб-контентом.

==================================================
ЛЕВАЯ БОКОВАЯ ПАНЕЛЬ
==================================================

Внутри открытого блокнота сделай постоянную левую вертикальную панель.

Требования:
- располагается слева;
- идет сверху вниз;
- овальной или сильно скругленной формы;
- занимает примерно одну шестую ширины экрана;
- не закрывает рабочую область листа;
- состоит только из значков;
- без подписей под значками;
- компактная, минималистичная;
- удобная для iPad;
- по духу интерфейса ближе к Procreate: ничего лишнего, быстрый доступ к функциям, рабочая область в приоритете.

Когда пользователь нажимает на значок:
- открывается список функций, относящихся к значку;
- повторное нажатие на тот же значок сворачивает список;
- интерфейс не должен быть постоянно раскрыт и захламлять экран;
- по возможности активным должен быть только один раскрытый блок за раз.

==================================================
СОСТАВ ЛЕВОЙ БОКОВОЙ ПАНЕЛИ
==================================================

Минимально нужны отдельные значки для:

1. Вставки
2. Ручки
3. Кисти и карандаши
4. Цвет листа
5. Закладки
6. Загрузка фото
7. Загрузка файла

==================================================
ВСТАВКИ / ФИГУРНЫЕ ЗАМЕТКИ
==================================================

Раздел "Вставки" должен позволять добавлять заметки/элементы в виде фигур.

Пользователь должен иметь возможность:
- выбрать форму;
- выбрать цвет;
- выбрать размер;
- растянуть вставку так, как ему нужно;
- разместить в любом месте листа;
- перемещать ее;
- позже менять размер;
- выбрать стиль бумаги для вставки;
- выбрать край:
  - ровно обрезанный;
  - криво оторванный.

Поддержать как минимум:
- прямоугольник;
- квадрат;
- круг/овал;
- облачко/выноску;
- стикер;
- бумажную заметку;
- карточку.

Вставки должны быть полноценными элементами страницы:
- их можно перемещать;
- их можно увеличивать и уменьшать;
- их можно перекрывать другими вставками;
- по ним можно рисовать ручкой, маркером, кистью;
- поверх них можно класть другие заметки, фото и элементы.

==================================================
РУЧКИ
==================================================

Отдельный значок для ручек.

При открытии должен показывать:
- каталог ручек;
- выбор цвета;
- выбор толщины;
- быстрый выбор пресетов;
- все доступные типы ручек и цветов.

==================================================
КИСТИ И КАРАНДАШИ
==================================================

Отдельный значок для:
- кистей;
- карандашей;
- художественных инструментов.

Должно поддерживаться:
- выбор типа;
- выбор цвета;
- выбор толщины;
- прозрачность, если уместно;
- все доступные пресеты.

==================================================
ЦВЕТ ЛИСТА
==================================================

Отдельный значок для выбора цвета текущего листа.

Важно:
- при смене цвета листа должны сохраняться:
  - линии;
  - клетка;
  - точки;
  - текстура;
  - рельеф бумаги;
- меняется только цвет основы бумаги, а не уничтожается тип бумаги.

==================================================
ЗАКЛАДКИ
==================================================

Отдельный значок для закладок.

Он должен:
- открывать список всех страниц, добавленных в закладки;
- позволять быстро перейти к выбранной странице;
- хранить закладки локально;
- работать без лишних служебных экранов.

Также добавь кнопку со значком звезды в правом нижнем углу страницы.

Поведение:
- нажатие добавляет текущую страницу в закладки;
- если страница уже в закладках, это должно быть визуально видно;
- состояние сохраняется после перезагрузки.

==================================================
ЗАГРУЗКА ФОТО
==================================================

Добавь отдельный значок загрузки фото в левой панели.

Он должен позволять:
- выбрать изображение с iPad или компьютера;
- вставить фото на страницу;
- сохранить локально;
- после вставки позволить перемещать и масштабировать изображение;
- после перезагрузки фото должно оставаться доступным.

==================================================
ЗАГРУЗКА ФАЙЛОВ
==================================================

Добавь отдельный значок загрузки файла в левой панели.

Он должен позволять:
- выбрать файл;
- прикрепить его к странице или блокноту в рамках текущей архитектуры;
- сохранить локально;
- восстановить после перезагрузки;
- не делать эту функцию глубоко спрятанной.

==================================================
РЕДАКТОР ДОЛЖЕН БЫТЬ ЧИСТЫМ
==================================================

На странице не должно быть лишних служебных надписей и панелей.

Нужно убрать с листа:
- "Текст страницы";
- "Рисование";
- "Очистить слой";
- и прочие технические подписи.

Лист должен быть визуально чистым.

==================================================
ЛАСТИК
==================================================

Не нужна отдельная заметная кнопка "Очистить слой".

Вместо этого:
- внизу по центру страницы добавь маленький значок ластика;
- при нажатии активный инструмент превращается в ластик;
- пользователь стирает вручную там, где нужно;
- это режим ластика, а не полная очистка всего слоя.

==================================================
ПЕРЕЛИСТЫВАНИЕ СТРАНИЦ
==================================================

Основной способ навигации по страницам — перелистывание.

На iPad / touch:
- перелистывание жестом;
- вперед и назад;
- ощущение работы с реальным блокнотом.

На компьютере:
- клик по правому нижнему углу страницы = вперед;
- клик по левому нижнему углу страницы = назад.

Если следующей страницы еще нет:
- она создается автоматически;
- наследует формат текущего блокнота:
  - тип бумаги;
  - цвет бумаги;
  - общий стиль страницы.

Важно:
- не использовать тяжелую анимацию, если она мешает производительности;
- приоритет — стабильность, плавность и ощущение листания.

==================================================
ОТКРЫТЫЙ БЛОКНОТ ДОЛЖЕН СОХРАНЯТЬ ВНЕШНИЙ ОБРАЗ
==================================================

Когда блокнот открыт, пользователь должен чувствовать, что это тот же блокнот, что был на главной странице.

Нужно сохранять в открытом блокноте:
- визуальную идентичность;
- стиль обложки;
- переплет;
- общее ощущение конкретного выбранного блокнота.

==================================================
ХРАНЕНИЕ ДАННЫХ
==================================================

Сохрани локальное хранение в IndexedDB.

После перезагрузки должны сохраняться:
- блокноты;
- страницы;
- текст;
- рисунки;
- выбранный стиль;
- выбранный тип бумаги;
- выбранный цвет бумаги;
- выбранный тип блокнота;
- внешний вид обложки;
- пользовательская обложка;
- переплет;
- фоны приложения;
- пользовательский фон приложения;
- сила blur/dim фона;
- вставки/фигуры;
- изображения;
- файлы;
- закладки;
- инструмент по умолчанию;
- пользовательские визуальные настройки.

Если для изображений и файлов нужны blob/object URL или другое представление, реализуй хранение и восстановление аккуратно.

==================================================
МОДЕЛИ ДАННЫХ
==================================================

Расширь модели данных так, чтобы поддерживались:

Для notebook:
- id
- title
- color
- stylePreset
- notebookType
- paperType
- defaultPaperColor
- defaultTool
- coverStyle / coverPreset
- coverImage
- bindingType
- createdAt
- updatedAt
- appearance/background settings, если уместно

Для page:
- id
- notebookId
- title при необходимости
- pageOrder / index
- paperType
- paperColor
- isBookmarked
- elements
- createdAt
- updatedAt
- визуальные настройки листа

Для page elements:
- text
- drawing
- image
- fileAttachment
- insertElement / shapeNote

Для insertElement / shapeNote:
- id
- shapeType
- position
- size
- color
- paperStyle
- edgeStyle
- zIndex
- текст внутри, если уместно
- и другие нужные поля для свободного редактирования

Для app settings:
- background preset
- custom background image
- background blur amount
- background dim amount

Также отдельно оформи:
- конфиги бумаги;
- конфиги инструментов;
- конфиги стилей;
- конфиги обложек;
- конфиги переплетов;
- конфиги фоновых сцен;
- конфиги вставок/фигур.

==================================================
UI-АРХИТЕКТУРА
==================================================

Нельзя делать все в одном компоненте.

Раздели минимум на такие части:
- экран блокнотов;
- настройка фона приложения;
- создание блокнота;
- выбор стиля;
- выбор бумаги;
- выбор обложки;
- выбор переплета;
- редактор страницы;
- левая боковая панель;
- раскрывающиеся панели по значкам;
- селектор ручек;
- селектор кистей и карандашей;
- выбор цвета листа;
- список закладок;
- загрузка фото;
- загрузка файлов;
- механика перелистывания страниц;
- рендер визуального макета блокнота на главной странице.

Вынеси:
- конфиги;
- типы;
- db-логику;
- UI-компоненты;
- утилиты;
- визуальные ассеты;
- стили.

==================================================
ЭТАПЫ РЕАЛИЗАЦИИ
==================================================

Работай по шагам.

Этап 1:
Проанализируй текущую структуру проекта и определи, что уже есть и что нужно расширить.

Этап 2:
Обнови дизайн-систему и главный экран:
- заголовок "Мои блокноты";
- кнопки "Создать" и "Настроить фон приложения";
- реальные визуальные блокноты вместо карточек.

Этап 3:
Добавь модели данных и конфиги:
- бумага;
- инструменты;
- стили;
- обложки;
- переплет;
- фоны;
- вставки;
- закладки.

Этап 4:
Сделай полноценный экран/модалку создания блокнота:
- стиль;
- тип;
- бумага;
- обложка;
- переплет;
- стартовый инструмент.

Этап 5:
Переделай открытие блокнота:
- сразу на первую пустую страницу;
- без промежуточных экранов;
- без лишних надписей.

Этап 6:
Сделай левую вертикальную панель значков и раскрывающиеся блоки функций.

Этап 7:
Усиль редактор:
- рисование;
- ручки;
- кисти;
- вставки;
- цвет листа;
- фото;
- файлы;
- закладки;
- ластик.

Этап 8:
Реализуй перелистывание страниц как основной способ навигации.

Этап 9:
Приведи всё к цельному UX и проверь локальное сохранение.

==================================================
КРИТЕРИИ ГОТОВНОСТИ
==================================================

Считать задачу выполненной только если:

1. Главная страница показывает:
- заголовок "Мои блокноты";
- кнопку "Создать";
- кнопку "Настроить фон приложения";
- список блокнотов в виде самих блокнотов, а не веб-карточек.

2. Все тексты интерфейса на русском языке.

3. Фон приложения выбирается из реальных сцен-превью, а не из абстрактных цветов.

4. Можно загрузить свой фон приложения.

5. Есть настройка blur/dim для фона.

6. При создании блокнота можно выбрать:
- название;
- цвет;
- стиль;
- тип блокнота;
- тип бумаги;
- внешний вид обложки;
- переплет;
- инструмент по умолчанию.

7. На главной странице блокнот показывает:
- форму;
- обложку;
- переплет;
- только название по центру, если оно есть;
- без лишних нижних подписей и метаданных.

8. При открытии блокнота сразу открывается пустая страница нужного формата.

9. Нет экрана "Страницы", "Файлы блокнота", "Создать страницу" как основного первого экрана.

10. Страница не скроллится вниз как сайт.

11. Есть левая вертикальная панель со значками без подписей.

12. По нажатию на значок его функции раскрываются, по повторному нажатию сворачиваются.

13. Есть вставки/заметки в виде фигур с:
- выбором формы;
- цветом;
- размером;
- перемещением;
- растягиванием;
- наложением на другие элементы.

14. Есть отдельные разделы для:
- ручек;
- кистей и карандашей;
- цвета листа;
- закладок;
- фото;
- файлов.

15. Есть значок ластика снизу по центру, который включает режим ручного стирания.

16. Есть закладки и кнопка-звезда для текущей страницы.

17. Страницы перелистываются:
- на iPad жестом;
- на компьютере кликом по нижним углам.

18. Новые страницы создаются через перелистывание и наследуют формат блокнота.

19. После перезагрузки всё сохраняется локально.

20. TypeScript проходит без ошибок.

21. Production build проходит.

==================================================
ЧТО ПОКАЗАТЬ В ИТОГЕ
==================================================

После реализации покажи:

1. Краткий анализ текущего проекта.
2. Какие файлы изменены.
3. Какие новые файлы добавлены.
4. Как расширены модели данных.
5. Как реализованы:
- новые фоны-сцены;
- blur/dim;
- реальные блокноты на главной странице;
- стили с символами и изображениями;
- новые типы блокнотов;
- обложки;
- переплет;
- прямое открытие на пустую страницу;
- левая боковая панель;
- вставки/фигурные заметки;
- ручки и кисти;
- цвет листа;
- закладки;
- фото;
- файлы;
- ластик;
- перелистывание страниц.

В конце обязательно выполни и покажи результат команд:
- npm run check
- npm run build

Если где-то браузерная офлайн-среда не позволяет идеальную реализацию, не удаляй функцию молча. Реализуй максимально близкий вариант и явно опиши компромисс в итоговом отчете.

==================================================
РАБОЧИЙ СТАТУС
==================================================

Выполненный шаг 1:
- расширена модель `AppSettings`:
  - `backgroundDimAmount`;
  - `backgroundBlurAmount`;
- сохранена совместимость с legacy-полем `overlayOpacity`;
- затемнение и размытие теперь применяются в `AppShell`;
- в настройке фона появились отдельные контролы для `dim` и `blur`;
- новые значения сохраняются локально и восстанавливаются после перезагрузки.

Следующий шаг по выбранной очередности:
- переделка главного экрана;
- переход от обычных карточек к визуально более реалистичным блокнотам.

Выполненный шаг 2:
- главный экран переделан в галерею блокнотов;
- вместо карточек используются визуальные объекты-блокноты;
- на главном экране сохранены:
  - форма;
  - обложка;
  - переплет;
  - центральное название, если оно есть;
- с самих блокнотов убраны лишние технические подписи и метаданные;
- визуальный рендер главного экрана стал ближе к требованию “набор реальных блокнотов”.

Следующий шаг по выбранной очередности:
- дальнейшая полировка фоновых сцен приложения;
- углубление визуального качества пресетов и их предпросмотра.

Выполненный шаг 3:
- фоновые пресеты переработаны как более явные сцены;
- сцены стали лучше различаться между собой по образу и атмосфере;
- предпросмотры в модалке стали крупнее и визуально выразительнее;
- блок настроек `dim/blur` отделён визуально и стал восприниматься как часть настройки сцены;
- шаг по сценам и их превью зафиксирован в коде и документации.

Следующий шаг по выбранной очередности:
- усиление экрана создания блокнота;
- более визуальный выбор стиля, обложки, переплета, бумаги и стартового инструмента.

Выполненный шаг 4:
- модалка создания блокнота усилена визуально;
- добавлен живой предпросмотр блокнота;
- секции выбора стали более структурированными и цельными;
- сохранена текущая архитектура и существующие picker-компоненты;
- шаг создания блокнота теперь лучше соответствует требованию roadmap про визуальный выбор, а не набор текстовых полей.

Следующий шаг по выбранной очередности:
- переделка открытия блокнота;
- уход от промежуточного первого экрана со списком страниц к прямому входу в рабочую страницу.

Выполненный шаг 5:
- маршрут открытия блокнота теперь сразу переводит пользователя в рабочую страницу;
- если в блокноте ещё нет страниц, первая страница создаётся автоматически и наследует формат блокнота;
- промежуточный экран со списком страниц больше не является основным первым экраном блокнота;
- экран со страницами, файлами и настройками сохранён как отдельный вспомогательный маршрут управления;
- после создания нового блокнота пользователь тоже попадает сразу в рабочую страницу.

Следующий шаг по выбранной очередности:
- переработка каркаса редактора;
- левая вертикальная панель без подписей и с более чистым, не “сайтовым” ощущением страницы.

Выполненный шаг 6:
- левая панель редактора переведена в режим иконок без визуальных подписей;
- по повторному нажатию на активную иконку секция теперь сворачивается;
- раскрытая секция оформлена как отдельная рельефная боковая панель с компактным заголовком и кнопкой закрытия;
- верхняя часть редактора стала компактнее и чище за счёт коротких статусных плашек вместо длинной служебной строки;
- сама страница стала ближе к виду блокнота, а не сайта: убраны лишние подписи над текстом и рисованием, нижние блоки изображений и файлов собраны в более компактный ряд.

Следующий шаг по выбранной очередности:
- усиление редактора как рабочего инструмента;
- режим ластика и дальнейшая подготовка к свободному layout-редактированию элементов страницы.

Выполненный шаг 7:
- левая rail-панель закреплена слева вертикально и не уходит в горизонтальную полосу сверху на узких размерах;
- сценарий с иконками теперь одинаково сохраняет левую колонку и на более компактной ширине;
- добавлен отдельный переключатель режима `Ластик` снизу по центру листа;
- повторное нажатие по `Ластику` возвращает пользователя к предыдущему инструменту рисования.

Следующий шаг по выбранной очередности:
- развитие свободного layout-редактирования;
- доведение изображений и файлов до более полного drag/resize-поведения внутри страницы.

Выполненный шаг 8:
- изображения и файлы выведены прямо на лист как самостоятельные элементы страницы;
- для изображений и файлов добавлены drag и resize внутри рабочей области листа;
- подпись изображения и заметка к файлу продолжают храниться в существующей локальной модели page elements;
- исправлен цикл autosave: обновление `page.updatedAt` больше не провоцирует новое автосохранение без пользовательских изменений;
- индикатор `Сохраняем...` больше не мигает постоянно и показывается только во время реальной записи.

Следующий шаг по выбранной очередности:
- дальнейшая полировка свободного layout-режима;
- улучшение наложения, управления и визуальной согласованности элементов страницы.

Выполненный шаг 9:
- лаги при перемещении вставок, изображений и файлов сняты за счёт отказа от записи в IndexedDB на каждом pointer move;
- drag и resize теперь живут на локальном draft-state в самих слоях, а коммит в базу происходит только по завершению действия;
- для текстов внутри shape-вставок, подписей изображений и заметок файлов сохранение вынесено в commit по blur;
- рядом с ластиком добавлена корзина для удаления объектов на листе;
- объект можно перетащить в корзину, а сама корзина подсвечивается в момент наведения.

Следующий шаг по выбранной очередности:
- дальнейшая полировка свободного layout-режима;
- более точное управление слоями, наложением и визуальной иерархией элементов страницы.

==================================================
GITHUB ИНФРАСТРУКТУРА
==================================================

Зафиксировано отдельным техническим шагом:

- проект вынесен в отдельный локальный git-репозиторий;
- репозиторий опубликован на GitHub;
- добавлен базовый workflow `CI`:
  - `npm run check`;
  - `npm run build`;
- добавлены шаблоны:
  - issue template для багов;
  - issue template для feature request;
  - pull request template;
- создана отдельная рабочая ветка для следующих изменений.
Выполненный шаг 10:
- верхний блок редактора с названием страницы и служебными действиями полностью убран, чтобы лист начинался сразу с рабочего содержимого;
- управление названием страницы, макетом и состоянием сохранения перенесено в левую секцию `Лист` и в компактную плавающую плашку на самом листе;
- для shape-вставок, изображений и файлов добавлен touch-friendly сценарий: долгое касание по объекту включает перенос прямо с листа;
- после долгого касания объект можно довести до корзины рядом с ластиком и удалить тем же жестом;
- iPad-сценарий стал спокойнее и ближе к живому блокноту без верхнего служебного бара.

Следующий шаг по выбранной очередности:
- дальнейшая полировка свободного layout-режима;
- более точное управление слоями, наложением и визуальной иерархией элементов страницы.
Выполненный шаг 11:
- редактор переведён на единый лист бумаги без длинной вертикальной сборки из отдельных блоков, поэтому во время письма и рисования экран больше не уезжает вниз;
- текст, рисунок Apple Pencil и свободные объекты теперь работают в одной общей рабочей области листа;
- сохранён сценарий клавиатурного ввода текста, но Pencil рисует поверх того же листа без отдельного нижнего canvas-блока;
- статус сохранения поднят выше, чтобы не перекрывать основной текст на листе;
- активный объект теперь поднимается выше остальных, а визуальная иерархия слоёв стала понятнее за счёт выделения и более аккуратного наложения;
- свободный layout-режим стал ближе к ощущению настоящего листа бумаги, а не страницы с прокручиваемыми секциями.

Следующий шаг по выбранной очередности:
- дальнейшая полировка сценария перелистывания;
- более точная логика перехода к следующему листу и автосоздания новых страниц без разрыва рабочего процесса.
Выполненный шаг 12:
- исправлена регрессия высоты editor-sheet: при открытии страницы снова отображается сам рабочий лист, а не только внешняя оболочка экрана;
- листу возвращена стабильная высота на desktop и на iPad-ширинах, чтобы единый paper-stage оставался видимым сразу после открытия;
- левая вертикальная rail-панель теперь закрыта по умолчанию и не раскрывается автоматически при входе в блокнот;
- переход из списка блокнотов по-прежнему ведёт сразу на рабочий лист, но дополнительные секции открываются только по нажатию на иконку;
- сценарий входа в страницу стал ближе к целевому UX roadmap: сначала пользователь видит сам лист бумаги, а не раскрытую служебную панель.

Следующий шаг по выбранной очередности:
- дальнейшая полировка перелистывания;
- более естественный переход к следующему листу и автосоздание страниц без разрыва рабочего процесса.
### Шаг 13. Перелистывание до нового листа и нижний page-curl

Выполнено в текущем цикле правок:

- рабочий лист растянут на всю доступную высоту редактора, чтобы доходить до нижней границы экрана, а не обрываться раньше;
- внизу листа появились визуальные углы перелистывания, а при переходе между страницами показывается нижний page-curl эффект;
- перелистывание вперёд теперь ведёт на следующую страницу, а если пользователь находится на последней странице, автоматически создаётся новый лист того же блокнота;
- перелистывание назад ведёт на предыдущую страницу без промежуточных экранов управления;
- нижняя подсказка справа показывает, будет ли это переход на следующую страницу или создание нового листа.

Следующий запланированный шаг:

- дальнейшая полировка page-turn UX;
- более аккуратное поведение текста на границе листа и подготовка к ещё более естественной работе с последовательностью страниц.
### Шаг 14. Полная высота листа и индикатор последовательности страниц

Выполнено в текущем цикле правок:

- исправлена высотная цепочка редактора от `AppShell` до `editor-sheet`, поэтому рабочий лист теперь заполняет всю доступную высоту и доходит до нижней границы экрана;
- убран лишний верхний отступ у редакторной секции, из-за которого лист визуально висел выше и не дотягивался до конца экрана;
- на самом листе появился индикатор `Лист N из M`, чтобы последовательность страниц ощущалась понятнее при перелистывании;
- нижняя часть листа получила дополнительную подложку и тень, усиливающие ощущение физического листа внутри блокнота.

Следующий запланированный шаг:

- дальнейшая полировка page-turn UX на iPad;
- более аккуратное поведение длинного текста на границе листа и подготовка к ещё более естественной работе с последовательностью страниц.
Дополнительный багфикс после шага 14:

- базовые контейнеры `html`, `body`, `#root`, `AppShell` и `app-shell__inner` переведены на явную полную высоту окна, а не только на `min-height`;
- благодаря этому вся высотная цепочка редактора теперь получает реальный размер viewport, и рабочий лист корректно дотягивается до нижней границы экрана;
- для контейнеров редактора добавлено `overflow: hidden`, чтобы внутренние авторазмеры не ломали растяжение листа.
Дополнительный A4-фикс после багфикса высоты:

- рабочий лист переведён в режим расчёта реальной A4-геометрии по размеру доступной области редактора;
- лист теперь центрируется внутри рабочей зоны и больше не висит сверху;
- при изменении размеров окна или рабочей области размеры листа пересчитываются автоматически с сохранением пропорций A4.
Дополнительный полноэкранный фикс листа:

- хлебные крошки убраны из самого редактора, чтобы верхняя часть экрана не съедала высоту рабочего листа;
- `editor-screen` переведён в полноэкранный режим с компенсацией внешних паддингов оболочки;
- центрирование маленького A4-блока убрано, и лист снова занимает всю доступную рабочую область редактора;
- рабочий лист теперь растягивается по ширине и высоте editor-area, а не остаётся маленьким блоком сверху.
Дополнительная корректировка после полноэкранного фикса:

- верхняя навигационная строка редактора возвращена;
- режим с отрицательными отступами у `editor-screen` убран, потому что он визуально притягивал лист к верхней части экрана;
- рабочая область редактора снова строится как `навигация сверху + лист на оставшуюся высоту`.
Дополнительное расширение editor-area:

- для экранов редактора введён отдельный полноширинный режим оболочки;
- внутренняя ширина `app-shell__inner` на маршрутах `/pages/...` больше не ограничивается узким центральным контейнером;
- левая rail-панель и раскрытая боковая колонка стали компактнее, чтобы лист получал больше реальной ширины экрана.
Дополнительная полировка геометрии листа:

- сокращены лишние внешние рамки вокруг листа, чтобы бумага занимала больше полезной площади;
- усилено ощущение физического листа за счёт обновлённой внешней кромки, внутреннего света и мягкой нижней тени;
- внутренние поля листа и текстовая зона подправлены так, чтобы контент выглядел естественнее внутри бумаги.
Дополнительный портретный режим для iPad:

- для вертикальной ориентации редактор переведён в отдельный layout, где лист получает почти всю рабочую область;
- rail-панель и боковая секция больше не съедают ширину листа в портретном режиме и показываются как плавающие слои поверх бумаги;
- внутренние отступы листа и размеры нижних элементов поджаты, чтобы полезная площадь бумаги в вертикальном положении была максимально большой.
Дополнительная корректировка компоновки листа:

- лист немного уменьшен внутри editor-area, чтобы вокруг бумаги появились аккуратные поля;
- в портретной ориентации rail-колонка снова располагается рядом с листом, а не поверх него;
- раскрытая боковая секция в вертикальном режиме тоже возвращена в соседнюю колонку;
- закладка на листе поднята выше, чтобы не конфликтовать с нижней кнопкой перехода на следующую страницу.
Дополнительная полировка жеста перелистывания:

- перелистывание страницы теперь работает обычным горизонтальным свайпом почти из любой части листа;
- блокировка жеста сохранена только для активных объектов и нижней графы управления, чтобы не конфликтовать с переносом, resize и кнопками;
- индикатор `Лист N из M` перенесён в нижнюю графу, чтобы нижняя зона выглядела как единая система управления листом.
Дополнительная полировка page-turn UX:

- во время горизонтального свайпа лист теперь получает промежуточный визуальный отклик ещё до фактического перехода;
- сам лист слегка поворачивается и смещается вслед за жестом;
- нижний curl-эффект усиливается по мере свайпа, поэтому перелистывание ощущается ближе к реальной странице блокнота.
Дополнительная корректировка page-turn:

- из механики перелистывания убран наклон всего листа;
- визуальный акцент перелистывания перенесён на мягкий подъём и переворот только уголка страницы;
- превью свайпа и финальная анимация стали спокойнее и ближе к реальному перевороту кончика бумажного листа.
Дополнительная корректировка удаления и page-turn:

- curl-эффект перелистывания дополнительно смягчён: уголок страницы стал меньше и спокойнее;
- по нажатию на корзину теперь можно удалить текущий лист целиком;
- после удаления редактор переводит пользователя на соседнюю страницу, а если лист был последним, автоматически создаёт новый пустой лист.
Дополнительный шаг по свободному layout-режиму и удалению листа:

- удаление текущего листа переведено в короткую анимацию ухода бумаги в корзину, без резкого confirm-диалога;
- свободные объекты на листе ограничены рабочей областью бумаги, поэтому их можно устойчиво перетаскивать и растягивать в любое место внутри листа;
- редактирование подписи изображения, заметки файла и текста вставки синхронизировано с локальным draft-состоянием, чтобы изменения не терялись после перемещения или resize.
Дополнительный UX-фикс по медиа и секции вставок:

- подпись под изображением на листе убрана, чтобы вставленная картинка занимала всю высоту своего блока и выглядела чище;
- после успешной вставки изображения, файла или фигурной вставки секция `Вставки` автоматически закрывается, чтобы не мешать дальнейшей работе с листом.
Дополнительный шаг по режиму ввода на iPad:

- Apple Pencil на основном листе переведён в чисто рукописный режим: касание пером не активирует печатный textarea-слой;
- клавиатурный ввод сохраняется только для касания пальцем или обычного клика, чтобы рукописные штрихи и текст, написанные Apple Pencil, оставались рисунком/почерком, а не превращались в печатный текст.
Дополнительное усиление свободного рукописного листа:

- на самом текстовом слое добавлена дополнительная блокировка Pencil pointer-событий, чтобы печатный textarea не перехватывал ввод пера;
- рукописный ввод Apple Pencil закреплён как свободный графический слой по всему листу, поэтому написанное и нарисованное остаётся в своей фактической позиции на бумаге.
Дополнительный багфикс по свободному рукописному листу:

- внутренняя строка-подсказка у текстового слоя убрана, чтобы не провоцировать старт ввода с верхней строки листа;
- сам печатный `textarea` переведён в режим `readOnly` по умолчанию и активируется только от касания пальцем или обычного клика;
- благодаря этому Apple Pencil остаётся только в рукописном/рисовальном сценарии и не должен превращать написанное от руки в печатный текст или переносить ввод в верх листа.
Дополнительный фикс по живому штриху и движению листа:

- для Pencil-штрихов, кроме маркера, убрана промежуточная прозрачность во время рисования, чтобы линия сразу выглядела выбранным цветом;
- лист, canvas и idle-состояние текстового слоя переведены в режим без touch-scroll и overscroll, чтобы при рукописном вводе страница не сдвигалась и не отвлекала движением вверх-вниз.
Дополнительный фикс по режиму ластика:

- при включении ластика редактор автоматически сбрасывает клавиатурный режим и активные выделения, чтобы Apple Pencil сразу переключался в стирание;
- в режиме ластика печатный слой отключён от pointer-событий, поэтому перо не должно упираться в текстовый `textarea` и должно работать как настоящий ластик по слою штрихов.
Дополнительный UX-шаг по размеру ластика:

- при включении ластика секция инструментов слева автоматически раскрывается;
- в левой боковой панели для активного ластика отображается отдельный ползунок `Размер ластика`, чтобы ширину стирания можно было менять прямо из вертикальной графы со значками.
Дополнительная корректировка управления ластиком:

- автoраскрытие боковой панели при включении ластика убрано;
- размер ластика перенесён в отдельную вертикальную шкалу слева внизу листа, чтобы ширину стирания можно было менять без открытия секции со значками.
Дополнительный багфикс по жесту перелистывания:

- для жеста page-turn добавлена блокировка после взаимодействия Apple Pencil;
- перелистывание страницы оставлено только для ручного жеста пальцем, чтобы Pencil использовался исключительно для письма, рисования и стирания по листу.
Дополнительная корректировка кнопки палитры:

- кнопка смены цвета пера перенесена прямо в левую вертикальную колонку со значками;
- палитра закреплена в самом низу этой колонки, чтобы работать как часть основной панели инструментов, а не как отдельный плавающий элемент на листе.
Дополнительный фикс открытия палитры:

- по нажатию на кнопку палитры теперь открывается маленькое встроенное окно рядом с колонкой, а не скрытый браузерный диалог;
- в этом окне доступны быстрые цвета пера и отдельный выбор своего цвета, поэтому смена цвета срабатывает прямо внутри интерфейса на iPad.
Дополнительный шаг по удалению блокнотов на главном экране:

- на экране `Мои блокноты` внизу по центру добавлена корзина для удаления ненужных блокнотов;
- блокнот можно зажать, перетащить в корзину и удалить с короткой бумажной анимацией ухода, как у листов в редакторе;
- удаление блокнота теперь каскадно очищает его страницы, элементы, штрихи, вложения и связанные локальные assets из IndexedDB.
Дополнительная корректировка статуса сохранения:

- плашка `Все изменения сохранены` перенесена изнутри листа в верхнюю правую часть экрана редактора;
- теперь она стоит на одном уровне со строкой пути и не перекрывает сам лист бумаги.
Дополнительный фикс по окну палитры:

- маленькое окно палитры теперь открывается поверх листа и не обрезается рабочей областью редактора;
- после выбора готового или своего цвета палитра автоматически закрывается, чтобы не мешать дальнейшему письму.
Дополнительный фикс по главному экрану:

- экран `Мои блокноты` переведен в режим без вертикального touch-scroll и overscroll-эффекта;
- главный экран больше не должен ездить вверх-вниз при обычных касаниях и свайпах, сохраняя спокойное поведение как у экранов блокнота.
Дополнительный багфикс по палитре:

- окно палитры вынесено в отдельный верхний слой поверх всего приложения, чтобы оно гарантированно было видно после нажатия на кнопку в левой колонке;
- выбор цвета по-прежнему сразу закрывает палитру после нажатия.
Дополнительный фикс по автосохранению:

- автосохранение листа переведено на более длинную паузу после последнего изменения, чтобы запись в IndexedDB происходила только после короткой тишины, а не во время самого письма или рисования;
- статус сохранения стал спокойнее: вместо резкого мигания `Сохраняем...` редактор держит мягкий черновой режим и подтверждает сохранение уже после завершения записи.
Дополнительный фикс по возврату на главный экран:

- у оболочки приложения отключен горизонтальный overscroll-назад, чтобы случайные левые свайпы не уводили на главный экран;
- переход к `Мои блокноты` теперь должен происходить только через явное нажатие на ссылку в верхней строке.
Дополнительный фикс по ластику:

- при включении ластика боковая серая секция больше не должна появляться, даже если до этого была открыта одна из левых секций;
- у плавающего контроля ластика убрана фоновая капсула, оставлен только сам вертикальный ползунок с лёгким числовым индикатором;
- максимальный размер ластика увеличен вдвое: с `40` до `80`.
Дополнительный фикс по свайп-навигации:

- внутри редактора заблокирована history-навигация жестом `POP`, поэтому боковые системные свайпы больше не должны переводить на главный экран и обратно;
- возврат к `Мои блокноты` в редакторе переведен на явный программный переход с `replace`, чтобы после выхода нельзя было пролистнуться вперёд обратно в лист.
Дополнительный фикс по переплету и сохранению блокнота:

- переплеты `кольца` и `спираль` усилены по контрасту и теперь заметнее читаются на главном экране и на экране блокнота;
- такой же переплет добавлен и на сам рабочий лист редактора, чтобы тип блокнота читался во всех основных экранах;
- после сохранения изменений в блокноте модалка закрывается с короткой задержкой, чтобы тач по `Сохранить` не проваливался в лежащий под ней экран и не открывал блокнот сразу после сохранения.
Дополнительный фикс по главному экрану:

- bootstrap-подготовка больше не создает демонстрационные блокноты автоматически;
- на экране `Мои блокноты` должны отображаться только реальные пользовательские блокноты, без стартовых макетов.
- если в IndexedDB уже остался старый автосозданный демо-блокнот и он ещё не менялся пользователем, приложение тихо очищает его при загрузке.
Дополнительный фикс по созданию блокнота:

- после нажатия `Создать` новый блокнот открывается сразу, без ожидания повторной загрузки главного экрана;
- короткая задержка закрытия модалки сохранена только для режима `Сохранить`, чтобы защита от тач-провала не замедляла создание нового блокнота.
- если пользователь не ввел название вручную, блокнот теперь создается с именем по умолчанию `Новый блокнот`, без молчаливой блокировки создания.
Дополнительное усиление PWA и офлайн-режима:

- service worker переведен на более надежную схему с отдельными static/runtime cache, offline fallback и прогревом актуальных production-assets из `index.html`;
- установленное приложение теперь может стабильнее запускаться с рабочего стола iPad без сети за счет возврата закэшированной оболочки приложения;
- регистрация service worker дополнена прогревом кэша и мягким обновлением при возврате в приложение или восстановлении сети;
- `manifest` и `index.html` усилены дополнительными PWA-метаданными для standalone-режима на iPad.
Дополнительный фикс по клавиатурному вводу на листе:

- при касании пальцем текстовый слой переводится в editable-состояние сразу на DOM-уровне до стандартной обработки браузера, чтобы курсор вставал в точку касания, а не уходил в верх листа;
- дублирующий `pointerdown` на `textarea` убран, чтобы активация клавиатуры и постановка курсора не срабатывали дважды.
Дополнительная корректировка сохранения:

- автосохранение редактора переведено на длинный интервал в 10 минут, чтобы сохранение не отвлекало во время письма и рисования;
- в верхней строке редактора добавлена явная кнопка `Сохранить`, расположенная по центру на одном уровне со строкой пути и статусом.
- статус сохранения больше не меняется визуально после каждого слова, буквы или штриха; он обновляется только при явном сохранении или ошибке.
Дополнительная корректировка палитры:

- в быстрой палитре первым цветом теперь стоит чёрный;
- стартовый цвет пера и остальных рисующих инструментов по умолчанию переведен на чёрный, чтобы редактор не открывался со случайным светлым оттенком.
Дополнительный фикс по рукописному штриху:

- слой рисования больше не очищает и не перерисовывает весь canvas на каждом локальном штрихе;
- активная рукописная буква и линия теперь рисуются на отдельном live-canvas поверх основного, чтобы письмо отдельными буквами не давало эффект визуального “затухания”.
Дополнительный фикс по оформлению текущей страницы:

- при переходе из листа в экран блокнота по названию теперь передается `pageId` страницы-источника;
- если в оформлении блокнота меняется цвет или тип бумаги, обновление применяется именно к той странице, с которой был открыт экран оформления, а не уходит только в настройки для следующих страниц.
Дополнительный фикс по микромиганию рукописного ввода:

- активный рукописный штрих больше не уходит в React-синхронизацию на каждом движении пера;
- очередной сегмент линии дорисовывается локально на canvas, а полный штрих попадает в состояние только после завершения жеста, чтобы короткое письмо по буквам не вызывало визуальное микро-мигание.
Дополнительное усиление офлайн-запуска на iPad:

- service worker теперь прогревает не только базовую оболочку, но и реальные уже загруженные ресурсы страницы, чтобы запуск с рабочего стола iPad без сети был стабильнее;
- регистрация service worker переведена на явный `scope`, а `manifest` и `index.html` используют base-aware пути, чтобы standalone-режим корректнее находил и офлайн-оболочку, и её ресурсы.
- дополнительно включён прогрев `Cache Storage` из самого окна приложения, чтобы уже загруженные ресурсы сохранялись офлайн даже в более капризных сценариях iPad Home Screen.
- в приложение добавлен маленький диагностический PWA-статус, который показывает режим запуска, наличие сети, активность service worker, контроль текущего окна и готовность офлайн-оболочки;
- регистрация service worker теперь дополнительно следит за `controllerchange`, чтобы при необходимости один раз перехватить текущее окно после установки и не оставлять standalone-запуск без активного контроллера.
